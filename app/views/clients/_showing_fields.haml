%fieldset
  .field
    = f.date_select :date 
    = f.collection_select :listing_id, Listing.all, :id, :address, :prompt => "Select an Listing"
    = f.hidden_field :_destroy
    = link_to "remove", '#', class: "remove_fields"